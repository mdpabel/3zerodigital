'use server';

export const loginAction = async (formData: FormData) => {
  return {
    message: 'success',
    success: true,
  };
};

export const signUpAction = async (formData: FormData) => {
  return {
    message: 'success',
    success: true,
  };
};
